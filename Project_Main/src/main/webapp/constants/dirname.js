const url = new URL(window.location.href)
const baseUrl = url.origin + "/Job_Project/";
const pageUrl = baseUrl + "page/";
const constantsUrl = baseUrl + "constants/";

export {url, baseUrl, pageUrl, constantsUrl}